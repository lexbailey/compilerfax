#!/usr/bin/env bash

ROOT=$PWD/alpine_root
OUTERUID=$(id -u $OUTERUSER)
OUTERGID=$(id -g $OUTERUSER)
env -i unshare -imnpuUCT --map-root --map-users $OUTERUID,500,1 --map-groups $OUTERGID,500,1 -R alpine_root -S 500 --wd /workdir /workdir/startup "$@"
