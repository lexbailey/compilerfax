#!/usr/bin/env bash

ROOT=$PWD/alpine_root
UID=$(id -u $OUTERUSER)
env -i unshare -imnpuUCT --map-root --map-users 500:$UID:1 --map-groups 500:$UID:1 -R alpine_root -S 500 --wd /workdir /workdir/startup "$@"
