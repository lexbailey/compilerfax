#!/usr/bin/env bash

ROOT=$PWD/alpine_root
MAPUID=$(id -u $OUTERUSER)
MAPGID=$(id -g $OUTERUSER)
unshare -imnpuUCT --map-root --map-users 1100:$MAPUID:1 --map-groups 1100:$MAPGID:1 -R alpine_root -S 1100 --wd /workdir /workdir/startup "$@"
